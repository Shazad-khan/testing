name: Continuous Deployment

on: push

jobs:
  my-job:
    name: My Job
    runs-on: ubuntu-latest
    steps:
    - name: Print a greeting
      run: |
        echo Hello there!
        env:
        CI: true
        runs-on: ubuntu-latest
        runs-on: ${{ matrix.os }}
        strategy:
          matrix:
          os: [ubuntu-20.04, ubuntu-18.04]
          node: [6, 8, 10]


